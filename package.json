{
  "name": "pack3-pagescms-poc",
  "private": true,
  "description": "Static (11ty) site with HTML split into partials, edited via Pages CMS code editor",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve --port=8084",
    "check": "npm run build && node check.js"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
